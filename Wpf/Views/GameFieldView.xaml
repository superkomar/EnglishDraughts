<UserControl x:Class="Wpf.Views.GameFieldView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:controls="clr-namespace:Wpf.Views.Controls"
      mc:Ignorable="d">

    <Grid>
        <Border BorderBrush="DimGray" BorderThickness="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <controls:GameField x:Name="GameField" IsEnabled="{Binding IsActive}"/>
        </Border>
    </Grid>
</UserControl>
